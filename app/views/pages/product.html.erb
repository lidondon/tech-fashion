<%= render 'carousel' %>
<div class="dropdown" style="margin-left:10px; margin-top:10px">
	<button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
		<%= @selected_category.name %>
		<span class="caret" />
	</button>
	<ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
		<% @categorys.try(:each) do |c| %>
			<li role="presentation">
				<%= link_to c.name, product_path(category_id: c.id) %>
			</li>
		<% end %>
	</ul>
</div>
<%= render 'product_list' %>
<!-- <div class="container">
	<div class="row">
		<div class="col-md-1">
			
		</div>
	</div>
	<div class="row">
		<div class="col-md-11">
			<%= render 'multiple_carousel', products: @products %>
		</div>
	</div>
</div> -->
			